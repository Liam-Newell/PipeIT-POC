platform: linux
image_resource:
  type: node
  source: ' # NOTE: It is important that this Docker image contains the cf command line utility as it is used by the task # which the Enterprise Pipeline intercepts and will be the context in which the deploy script will run'
  repository: sdf
inputs:
  - name: source
outputs:
  - name: target
run:
  path: source/concourse/scripts/assemble.sh
